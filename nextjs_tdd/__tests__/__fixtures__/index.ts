import { faker } from '@faker-js/faker';

export const mockChildrenProps = {
    children: faker.lorem.sentence(),
    airlines: faker.lorem.sentence(),
    home: faker.lorem.sentence()
};

export const mockContextProps = {
    children: faker.lorem.sentence()
};
